<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
  <packaging>rpm</packaging>
  <modelVersion>4.0.0</modelVersion>
  <artifactId>cfe_16</artifactId>
  <version>${revision}${sha1}${changelist}</version>
  <name>cfe_16</name>
  <description>cfe_16</description>
  <groupId>com.teragrep</groupId>
  <url>https://teragrep.com</url>
  <properties>
    <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
    <maven.compiler.source>1.8</maven.compiler.source>
    <maven.compiler.target>1.8</maven.compiler.target>
    <java.version>1.8</java.version>
    <revision>0.0.1</revision>
    <changelist>-SNAPSHOT</changelist>
    <sha1/>
  </properties>
  <scm>
    <connection>scm:git:https://github.com/teragrep/cfe_16.git</connection>
    <developerConnection>scm:git:git@github.com:teragrep/cfe_16.git</developerConnection>
    <url>https://github.com/teragrep/cfe_16/tree/master</url>
  </scm>
  <build>
    <directory>${project.basedir}/target</directory>
    <plugins>
      <plugin>
        <artifactId>maven-enforcer-plugin</artifactId>
        <version>3.4.1</version>
        <executions>
          <execution>
            <id>enforce</id>
            <phase>none</phase>
          </execution>
          <execution>
            <id>enforce-maven</id>
            <goals>
              <goal>enforce</goal>
            </goals>
            <configuration>
              <rules>
                <requireMavenVersion>
                  <version>3.2.5</version>
                </requireMavenVersion>
                <requireJavaVersion>
                  <version>1.8</version>
                </requireJavaVersion>
              </rules>
            </configuration>
          </execution>
        </executions>
      </plugin>
      <plugin>
        <groupId>org.codehaus.mojo</groupId>
        <artifactId>rpm-maven-plugin</artifactId>
        <version>2.2.0</version>
        <extensions>true</extensions>
        <executions>
          <execution>
            <id>default-rpm</id>
            <goals>
              <goal>rpm</goal>
            </goals>
            <phase>package</phase>
          </execution>
        </executions>
        <configuration>
          <summary>${project.groupId}-${project.artifactId}</summary>
          <name>${project.groupId}-${project.artifactId}</name>
          <version>${project.version}</version>
          <release>${env.GITHUB_RUN_NUMBER}</release>
          <license>Proprietary</license>
          <distribution>Fail-Safe Log Management Suite</distribution>
          <vendor>https://fail-safe.net/</vendor>
          <packager>Fail-Safe &lt;servicedesk@fail-safe.net&gt;</packager>
          <group>Fail-Safe/LogManagementSuite</group>
          <description>${project.groupId}-${project.artifactId}</description>
          <needarch>false</needarch>
          <defaultUsername>srv-cfe_16</defaultUsername>
          <defaultGroupname>srv-cfe_16</defaultGroupname>
          <defaultFilemode>0644</defaultFilemode>
          <defaultDirmode>0755</defaultDirmode>
          <defineStatements>
            <defineStatement>_build_id_links none</defineStatement>
          </defineStatements>
          <mappings>
            <mapping>
              <directory>/opt/teragrep/${project.artifactId}/lib</directory>
              <directoryIncluded>true</directoryIncluded>
              <filemode>755</filemode>
              <dirmode>755</dirmode>
              <username>srv-cfe_16</username>
              <groupname>srv-cfe_16</groupname>
              <recurseDirectories>true</recurseDirectories>
              <sources>
                <source>
                  <location>${project.basedir}/../target/cfe_16.jar</location>
                </source>
              </sources>
            </mapping>
          </mappings>
          <requires>
            <require>java-1.8.0-openjdk-headless</require>
          </requires>
          <preinstallScriptlet>
            <script>
getent group srv-cfe_16 > /dev/null || groupadd -r srv-cfe_16;
getent passwd srv-cfe_16 > /dev/null || useradd -r -g srv-cfe_16 -d /opt/teragrep/${project.artifactId} -s /sbin/nologin -c "Service User" srv-cfe_16;
exit 0;
            </script>
          </preinstallScriptlet>
        </configuration>
      </plugin>
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-surefire-plugin</artifactId>
        <version>3.2.5</version>
        <configuration>
          <skipTests>true</skipTests>
        </configuration>
      </plugin>
    </plugins>
  </build>
  <distributionManagement>
    <repository>
      <id>github</id>
      <url>https://maven.pkg.github.com/${env.GITHUB_REPOSITORY}</url>
    </repository>
  </distributionManagement>
</project>
